---
import Layout from '../layouts/Layout.astro';
import { clientEnv } from '@shared/config/env';
import HomePageClient from '@client/components/pages/HomePageClient';

// SEO metadata
const title = `${clientEnv.APP_NAME} - Scale Your Organic Traffic on Autopilot`;
const description = `AI content that ranks and reads human. Set it, forget it, watch traffic grow. Multi-model AI, built-in humanizer, and one-click CMS publishing.`;
---

<Layout title={title}>
  <!-- SEO Meta Tags in head slot -->
  <Fragment slot="head">
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={`${clientEnv.BASE_URL}/`} />
    <meta property="og:site_name" content={clientEnv.APP_NAME} />
    <meta property="og:image" content={`${clientEnv.BASE_URL}/og-image.png`} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={`${clientEnv.BASE_URL}/og-image.png`} />
    <link rel="canonical" href={`${clientEnv.BASE_URL}/`} />

    <!-- Schema.org JSON-LD -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      "name": clientEnv.APP_NAME,
      "applicationCategory": "BusinessApplication",
      "operatingSystem": "Web",
      "offers": {
        "@type": "Offer",
        "price": "49.00",
        "priceCurrency": "USD",
        "priceSpecification": {
          "@type": "UnitPriceSpecification",
          "billingDuration": "P1M"
        }
      },
      "aggregateRating": {
        "@type": "AggregateRating",
        "ratingValue": "4.8",
        "ratingCount": "12"
      }
    })}></script>
  </Fragment>

  <!-- React island for interactive landing page -->
  <HomePageClient client:load />
</Layout>
